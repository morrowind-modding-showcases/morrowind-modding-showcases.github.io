name: Validate site

on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-site-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install site and wiki dependencies
        run: |
          npm ci
          npm ci --prefix wiki

      - name: Build and validate content
        run: |
          npm run content:build
          npm run content:check
          npm run validate:wiki
          npm test
          npm run build:site
